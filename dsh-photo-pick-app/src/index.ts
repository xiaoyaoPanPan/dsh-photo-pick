/**
 * `dsh-photo-pick-app` — installable profile bundle for photo ranking.
 * The package substance is `cordis.patch.yml` (`dsh.bundle.patch`); this module
 * carries no runtime API.
 * @module dsh-photo-pick-app
 */

export {}
