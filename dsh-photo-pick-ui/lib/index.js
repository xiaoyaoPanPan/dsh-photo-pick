//#region src/index.ts
/** Host loader entry for the browser implementation exported from `./client`. */
/** Host plugin body — settings UI lives entirely in the browser half. */
function apply() {}
//#endregion
export { apply };
